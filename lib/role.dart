import 'package:flutter/material.dart';

class role extends StatelessWidget{
  @override
  Widget build (BuildContext context){
    //adjust role size  
    return Container(
      height: 60,
      width: 60,
      child: Image.asset('image/role.png'),
    );
  }
}